#include "main.h"

/**
 * _islower - checks if the character
 * @c: a character argument
 * Return: return 0 and 1 depending on the character
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
