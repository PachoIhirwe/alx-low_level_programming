#include "main.h"

/**
 * factorial - prints a string
 * @n: str to be printed
 *
 * Return: The lenth str
 */
int factorial(int n)
{
	int result = n;

	if (n < 0)
	{
		return (-1);
	}
	else if (n >= 0 && n <= 1)
	{
		return (1);
	}
}
