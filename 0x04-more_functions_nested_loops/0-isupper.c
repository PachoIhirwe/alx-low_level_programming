#include "main.h"

/**
 * _isupper - checks if uppercase
 * @c: variable text
 * Return: 1 or 0 depending
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
}
}
